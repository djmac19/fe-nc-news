import React from "react";

function Page({ updateQueries }) {
  return (
    <section>
      <button>Prev Page</button>
      <button> Next Page </button>
    </section>
  );
}

export default Page;
